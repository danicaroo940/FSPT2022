// Ejercicio JS => Cajero automático (4.5 PUNTOS)

// IMPORTANTE! Restaré puntos por usar cosas NO vistas en clase.
// Restaré (poco) por cada ; que falte, comillas dobles, etc
// El uso de cosas prohibidas como var conlleva pena de muerte

// 2.5 PUNTOS
  // 1) Todo el código debe ir encapsulado en una función
  // 2) A la función se le debe pasar un argumento numérico
  // 3) La función recibe por parámetro el importe que queremos sacar del cajero
  // 4) La función no debe devolver ningún resultado
  // 5) La función debe calcular el menor número de monedas y billetes
    // que compongan el importe recibido por parámetro
  // 6) Se imprimirá por consola cada uno de los billetes o monedas calculados
    // Para el ejemplo de 1410.23€ sería:
      // 500
      // 500
      // 200
      // 200
      // 10
      // 0.2
      // 0.02
      // 0.01

// 1 PUNTO
  // 1) Que el log se vea diferenciado entre billetes y monedas
    // Por ejemplo "Se ha retirado un billete de 500€"
    // o "Se ha retirado una moneda de 1€"

// 1 PUNTO
  // 1) Que por cada billete y/o moneda sacado por consola, aparezcan 2 logs más
    // diciendo el importe que queda por sacar y ya extraido. Por ejemplo:
    // Queda por sacar 1410.23€
    // 1 billete de 500€
    // Se han sacado 500€
    // ----------------------------------------------------
    // Queda por sacar 910.23€
    // 1 billete de 500€
    // Se ha sacado 1000€
